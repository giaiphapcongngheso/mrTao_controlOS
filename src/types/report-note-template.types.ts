export interface ReportNoteTemplate {
  id: string;
  title: string;       // Ten mau, vi du: "Ca binh thuong"
  content: string;     // Noi dung mau
  createdAt: string;   // ISO timestamp
  createdBy: string;   // fullName nguoi tao
}
